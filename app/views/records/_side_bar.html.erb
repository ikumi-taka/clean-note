<div class="side-bar-header">
  <div class="header-name">
    <%= link_to current_user.name, edit_user_path(current_user) %>
  </div>
  <div class="create-housework">
    <%= link_to "掃除記録を作成", new_housework_path %>
  </div>
</div>

<div class="houseworks">
  <% current_user.houseworks.each do |housework| %>
    <div class="housework">
      <div class="housework-name">
        <%= link_to housework.housework_name, housework_records_path(housework) %>
      </div>
    </div>
  <% end %>
</div>